# Contexte projet – Reprise urgente

Bonjour. Je reprends une conversation qui a mal tourné avec un autre assistant. J'ai besoin d'une solution directe.

## Structure du projet
```
S:\Python-Project\Zaer\
├── main.py
├── ui/
│   ├── main_window.py          # ← LE FICHIER QUI A ÉTÉ MODIFIÉ
│   └── windows/
│       └── saisie_consultation_ressources.py  # ← CE FICHIER EXISTE
└── database/
    ├── db_session.py
    └── models.py
```

## Le problème
Quand je lance `python main.py`, j'obtiens :
```
⚠️ ATTENTION : saisie_consultation_ressources.py introuvable !
```
Pourtant le fichier **existe bien** dans `ui/windows/`.

**Ce qui s'est passé :**
- L'assistant précédent m'a fait vider le cache (`__pycache__`).
- Avant, ça marchait. Depuis, plus rien.
- L'assistant précédent a modifié `ui/main_window.py`.
- Je n'ai pas d'éditeur de code, je dois corriger sans ouvrir les fichiers ou en utilisant le terminal uniquement.

## Ce que je veux
1. Une **commande terminal** à taper pour corriger `main_window.py` (ou pour lancer le programme) sans avoir à éditer le fichier manuellement.
2. Si la correction passe par une commande `echo` ou `powershell` pour modifier la ligne dans `main_window.py`, donnez-la moi.
3. Ne me donnez pas de README, ne me donnez pas de bla-bla technique. Donnez-moi juste **la commande magique** à taper dans le terminal pour que ça refonctionne tout de suite.

J'attends votre solution directe.